input: 
	dataHi[size](NumericArray),
	dataLo[size](NumericArray),
	dataClose[size](NumericArray),
	index(NumericSimple);
array:
	intrabarpersist trCandidate[3](0);
begin
	trCandidate[0] = absvalue(dataHi[index] - dataLo[index]);
	trCandidate[1] = absvalue(dataHi[index] - dataClose[index-1]);
	trCandidate[2] = absvalue(dataLo[index] - dataClose[index-1]);
	myTR = myExtremesArray(trCandidate, 2, true, 3);
end;
