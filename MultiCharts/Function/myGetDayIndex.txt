input: 
	dayBarIndex(NumericRef);
var: 
	intrabarpersist lastUpdateDate(0);
begin
	if Date <> lastUpdateDate and Date <> Date[1] then 
	begin 
		dayBarIndex = dayBarIndex + 1;
		lastUpdateDate = Date;
	end;
end;
