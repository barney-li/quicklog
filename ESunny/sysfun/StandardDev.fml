//--------------------------------------------------------------
// 简称: StandardDev
// 名称: 求标准差
// 类型: 用户函数
// 输出: Numeric
//--------------------------------------------------------------
Params
	NumericSeries Price(1);
	Numeric Length(10);
	Numeric DataType(1);
Vars
	Numeric VarPSValue;
Begin
	VarPSValue = VariancePS(Price, Length, DataType);
	If (VarPSValue > 0)
	{
		Return Sqrt(VarPSValue);
	}Else
	{
		Return 0;
	}
End
