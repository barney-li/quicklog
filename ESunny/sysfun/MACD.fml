//--------------------------------------------------------------
// 简称: MACD
// 名称: 计算平滑异同移动平均
// 类型: 用户函数
// 输出: Numeric
//--------------------------------------------------------------
Params
	NumericSeries Price(10);
	NumericRef DIFF;
	NumericRef DEA;
	Integer prdShort(12);
	Integer prdLong(26);
	Integer Length(9);

Begin
	DIFF = XAverage(Price,prdShort) - XAverage(Price,prdLong);
	DEA  = XAverage(DIFF,Length);
	Return 2 * (DIFF - DEA);	
	
End
